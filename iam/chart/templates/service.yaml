apiVersion: v1
kind: Service
metadata:
  name: iam
spec:
  type: NodePort
  ports:
    - name: iam
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31060
  selector:
    application: iam
